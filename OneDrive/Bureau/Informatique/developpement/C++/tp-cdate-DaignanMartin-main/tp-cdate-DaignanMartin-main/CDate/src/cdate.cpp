#include "cdate.h"

CDate::CDate(int un_mois,int une_annee,int un_jour,int une_minute,int une_seconde,int une_heure){
    this -> mois = un_mois;
    this -> annee = une_annee;
    this -> jour = un_jour;
    this -> minute = une_minute;
    this -> seconde = une_seconde;
    this -> heure = une_heure;
}
CDate::CDate(){
}
void CDate:: ajouter_une_seconde(){
}
void CDate:: recuperer_valeur(int &un_mois,int &une_annee,int &un_jour,int &une_minute,int &une_seconde,int &une_heure){
    this -> mois = un_mois;
    this -> annee = une_annee;
    this -> jour = un_jour;
    this -> minute = une_minute;
    this -> seconde = une_seconde;
    this -> heure = une_heure;

}
bool CDate::operator > (CDate date){
if(this -> CDate() > CDate(){

return true;
}
}
bool CDate:: operator < (CDate date){
if(this -> CDate() < CDate(){

return true;
}
}
bool CDate:: operator == (CDate date)
{
if(this -> CDate() == CDate(){

return true;
}

}
